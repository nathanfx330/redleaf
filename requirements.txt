# --- Redleaf Engine Python Dependencies ---
#
# To install all the required libraries, run the following command in your terminal:
# pip install -r requirements.txt
#
# After installing these, you must also download the required spaCy model.
#
# == System-Level Dependencies ==
#
# 1. sqlite3 (Command-Line Tool)
#    - WHY: Required for the `export-precomputed-state` command.
#    - HOW TO CHECK: Run `sqlite3 --version`.
#    - HOW TO INSTALL: Varies by OS (e.g., `sudo apt-get install sqlite3` on Ubuntu).

# == Core Web Framework ==
Flask==2.3.3
Flask-WTF==1.1.1
Werkzeug==2.3.7

# == Document Processing & Data Extraction ==
beautifulsoup4==4.12.2
lxml==4.9.3
PyMuPDF==1.23.5

# == Natural Language Processing (NLP) ==
spacy==3.6.1
ollama

# == Synthesis & Exporting ==
odfpy==1.4.1
citeproc-py==0.6.0

# == API Requests ==
requests==2.31.0

# == Curator CLI & Database ==
duckdb
pytz

# == CLI & User Experience ==
tqdm

# --- IMPORTANT: spaCy Model and GPU Support ---
#
# After running 'pip install -r requirements.txt', you MUST download the model with:
#
#    python -m spacy download en_core_web_lg
#
# (OPTIONAL) FOR GPU ACCELERATION:
#    pip install cupy-cuda11x  # or cupy-cuda12x depending on your CUDA version