# ===================================================================
# --- Redleaf Project Artifacts ---
# ===================================================================

# The user's private document source folder
/documents/

# User's Node Leaf project saves and Agentic Wiki files
# (Tip: To version control your Wiki, run 'git init' inside the Wiki folder itself!)
/Node-leaf-saves/

# Downloaded AI/NLP models (Too large for Git)
/models/

# The live, private SQLite database file and its temporary files.
# The public, pre-computed version is initial_state.sql.zip, which is NOT ignored.
/knowledge_base.db
/knowledge_base.db-shm
/knowledge_base.db-wal

# The DuckDB workspace and batch state for the curator pipeline.
# The wildcard (*) covers .duckdb, .duckdb.wal, and .duckdb.tmp files.
/curator_workspace.duckdb*
/curator_batch_state.json

# The instance folder contains the secret key and temporary user uploads.
# We ignore specific private files within it, NOT the whole folder.
/instance/secret.key
/instance/uploads/
/instance/contributions/

# Log files and temporary NLP data generated by the pipelines.
/curator_pipeline.log
/temp_nlp_results/

# Chat logs exported from the AI assistant.
/chats/

# Assistant configuration file.
.assistant_config.json


# ===================================================================
# --- Standard Python & Virtual Environment ---
# ===================================================================

# Python cache and artifacts
__pycache__/
*.py[cod]

# Virtual Environment
/venv/
/.venv/
/env/
/.env/
.Python

# Distribution / packaging
/build/
/develop-eggs/
/dist/
/downloads/
/eggs/
/.eggs/
/lib/
/lib64/
/parts/
/sdist/
/var/
/wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST


# ===================================================================
# --- OS-Specific & IDE Files ---
# ===================================================================
.DS_Store
Thumbs.db
/.vscode/
/.idea/